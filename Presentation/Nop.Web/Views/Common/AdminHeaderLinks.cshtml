@model AdminHeaderLinksModel
@using Nop.Web.Models.Common;
@if (Model.IsCustomerImpersonated || Model.DisplayAdminLink)
{
    <div class="admin-header-links text-center">
        @if (Model.IsCustomerImpersonated)
        {
            <div class="impersonate">
                <span>@string.Format(T("Account.ImpersonatedAs").Text, Model.ImpersonatedCustomerEmailUsername)</span>
                <a href="@Url.RouteUrl("Logout")" class="finish-impersonation" title="@T("Account.ImpersonatedAs.Finish.Hint")">@T("Account.ImpersonatedAs.Finish")</a>
            </div>
        }
        @if (Model.DisplayAdminLink)
        {
            <div id="notice-bar" class="notice-bar section-full-width">
                <div class="notice-bar-content content-inner-width">
                    <p><a href="@Url.Content("~/admin")" class="administration">@T("Account.Administration")</a></p>
                </div>
            </div>
        }
    </div>
}
@*<div id="notice-bar" class="notice-bar section-full-width">
        <div class="notice-bar-content content-inner-width">
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit <a href="#">Click</a></p>
        </div>

    </div>*@
@*<a href="@Url.Content("~/admin")" class="administration">@T("Account.Administration")</a>*@
